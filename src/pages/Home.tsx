import Layout from '@/components/Layouts';
import ShippingForm from '@/components/ShippingForm';

const Home = () => {
  return (
    <Layout>
      <ShippingForm />
    </Layout>
  );
};

export default Home;
